spring.application.name=devicecontrolapi

# Clave de JWT
jwt.secret=mi_clave_super_secreta_12345

# Configuraci�n de la base de datos remota
# spring.datasource.url=jdbc:mysql://213.136.93.169:3306/ki829222_devicecontrol_pruebas?useSSL=false&serverTimezone=UTC
# spring.datasource.username=ki829222_admindc
# spring.datasource.password=devicecontrol192*

# Configuraci�n de la base de datos local
spring.datasource.url=jdbc:mysql://localhost:3306/devicecontrol?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=

# Driver de la base de datos (esto es espec�fico para MySQL)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Habilitar el uso de JPA (Hibernate)
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Habilitar o deshabilitar el "DDL" autom�tico (esto depende de si quieres que Spring administre la creaci�n de tablas autom�ticamente)
#spring.jpa.hibernate.ddl-auto=none   # Puedes usar 'update', 'create', 'create-drop', etc.
spring.jpa.hibernate.ddl-auto=update

# Mostrar las consultas SQL en la consola (opcional)
spring.jpa.show-sql=true

# Formatear las consultas SQL (opcional)
spring.jpa.properties.hibernate.format_sql=true
